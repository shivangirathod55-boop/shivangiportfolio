/* =========================================================
   SHIVANGI PORTFOLIO
   Mobile-first responsive stylesheet
========================================================= */


/* =========================================================
   1. CSS VARIABLES & RESET
========================================================= */

:root {
    --color-primary: #10152f;
    --color-primary-soft: #1c2450;

    --color-accent: #ff5c8a;
    --color-accent-dark: #e84574;

    --color-secondary: #7357ff;
    --color-secondary-light: #9a7cff;

    --color-highlight: #18c8b5;

    --color-white: #ffffff;
    --color-off-white: #f8f8fc;
    --color-light: #f1f2f8;
    --color-text: #20233d;
    --color-text-soft: #686b80;
    --color-border: rgba(16, 21, 47, 0.09);

    --gradient-main: linear-gradient(
        135deg,
        #7357ff 0%,
        #ff5c8a 100%
    );

    --gradient-soft: linear-gradient(
        135deg,
        rgba(115, 87, 255, 0.12),
        rgba(255, 92, 138, 0.10)
    );

    --gradient-dark: linear-gradient(
        135deg,
        #10152f 0%,
        #252c5a 100%
    );

    --shadow-soft: 0 15px 45px rgba(16, 21, 47, 0.08);
    --shadow-card: 0 20px 55px rgba(16, 21, 47, 0.10);
    --shadow-hover: 0 25px 60px rgba(16, 21, 47, 0.15);

    --radius-sm: 12px;
    --radius-md: 20px;
    --radius-lg: 30px;
    --radius-xl: 38px;

    --container-width: 1180px;

    --transition: 0.3s ease;
}


* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}


html {
    scroll-behavior: smooth;
    scroll-padding-top: 80px;
}


body {
    min-width: 0;
    overflow-x: hidden;

    font-family:
        Inter,
        "Segoe UI",
        Roboto,
        Helvetica,
        Arial,
        sans-serif;

    background: var(--color-off-white);
    color: var(--color-text);

    line-height: 1.7;
    -webkit-font-smoothing: antialiased;
}


img {
    display: block;
    max-width: 100%;
}


a {
    color: inherit;
    text-decoration: none;
}


button,
a {
    -webkit-tap-highlight-color: transparent;
}


button {
    font: inherit;
}


ul {
    list-style: none;
}


.container {
    width: min(
        calc(100% - 32px),
        var(--container-width)
    );

    margin-inline: auto;
}


.section {
    position: relative;
    padding: 78px 0;
    overflow: hidden;
}


::selection {
    background: var(--color-accent);
    color: var(--color-white);
}


/* =========================================================
   2. NAVIGATION
========================================================= */

.site-header {
    position: fixed;
    top: 0;
    left: 0;

    width: 100%;
    z-index: 1000;

    background: rgba(248, 248, 252, 0.88);
    backdrop-filter: blur(18px);
    -webkit-backdrop-filter: blur(18px);

    border-bottom: 1px solid rgba(16, 21, 47, 0.06);
}


.navbar {
    min-height: 72px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    position: relative;
}


.logo {
    display: inline-flex;
    align-items: center;
    gap: 10px;

    font-size: 1.05rem;
    font-weight: 800;
    letter-spacing: -0.02em;

    color: var(--color-primary);
}


.logo-mark {
    width: 38px;
    height: 38px;

    display: grid;
    place-items: center;

    border-radius: 12px;

    background: var(--gradient-main);
    color: var(--color-white);

    font-size: 1rem;
    font-weight: 800;

    box-shadow: 0 8px 22px rgba(115, 87, 255, 0.22);
}


.menu-toggle {
    width: 46px;
    height: 46px;

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 5px;

    border: 1px solid var(--color-border);
    border-radius: 13px;

    background: rgba(255, 255, 255, 0.8);
    color: var(--color-primary);

    cursor: pointer;
}


.menu-toggle span {
    width: 19px;
    height: 2px;

    border-radius: 10px;
    background: currentColor;

    transition: transform var(--transition),
                opacity var(--transition);
}


.nav-links {
    position: absolute;

    top: calc(100% + 10px);
    left: 0;
    right: 0;

    display: flex;
    flex-direction: column;
    gap: 4px;

    padding: 10px;

    background: rgba(255, 255, 255, 0.96);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);

    border: 1px solid var(--color-border);
    border-radius: 20px;

    box-shadow: var(--shadow-card);

    opacity: 0;
    visibility: hidden;
    transform: translateY(-8px);

    transition:
        opacity var(--transition),
        visibility var(--transition),
        transform var(--transition);
}


.nav-links.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}


.nav-links a {
    display: block;

    padding: 12px 14px;

    border-radius: 12px;

    color: var(--color-text-soft);

    font-size: 0.92rem;
    font-weight: 600;

    transition:
        background var(--transition),
        color var(--transition);
}


.nav-links a:hover,
.nav-links a:focus-visible {
    background: var(--gradient-soft);
    color: var(--color-primary);
}


/* =========================================================
   3. HERO
========================================================= */

.hero {
    min-height: 100svh;

    display: flex;
    align-items: center;

    padding-top: 120px;
    padding-bottom: 70px;

    background:
        radial-gradient(
            circle at 90% 10%,
            rgba(115, 87, 255, 0.15),
            transparent 30%
        ),
        radial-gradient(
            circle at 5% 85%,
            rgba(255, 92, 138, 0.13),
            transparent 28%
        ),
        var(--color-off-white);
}


.hero-content {
    display: grid;
    gap: 54px;
}


.hero-text {
    position: relative;
    z-index: 2;
}


.eyebrow,
.section-label {
    display: inline-flex;
    align-items: center;
    gap: 8px;

    margin-bottom: 14px;

    color: var(--color-secondary);

    font-size: 0.74rem;
    font-weight: 800;

    letter-spacing: 0.13em;
    text-transform: uppercase;
}


.eyebrow::before,
.section-label::before {
    content: "";

    width: 22px;
    height: 2px;

    border-radius: 10px;

    background: var(--gradient-main);
}


.hero h1 {
    max-width: 700px;

    color: var(--color-primary);

    font-size: clamp(3rem, 15vw, 6.5rem);
    line-height: 0.98;
    letter-spacing: -0.065em;

    margin-bottom: 22px;
}


.hero h1 span {
    display: block;

    background: var(--gradient-main);
    background-clip: text;
    -webkit-background-clip: text;
    color: transparent;
}


.hero-tagline {
    max-width: 620px;

    margin-bottom: 13px;

    color: var(--color-text);

    font-size: clamp(1.15rem, 4.8vw, 1.65rem);
    line-height: 1.45;
    font-weight: 650;

    letter-spacing: -0.02em;
}


.hero-subtitle {
    max-width: 580px;

    margin-bottom: 30px;

    color: var(--color-text-soft);

    font-size: 0.98rem;
}


.btn {
    min-height: 52px;

    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;

    padding: 13px 20px;

    border-radius: 14px;

    font-size: 0.92rem;
    font-weight: 750;

    transition:
        transform var(--transition),
        box-shadow var(--transition),
        background var(--transition);
}


.btn-primary {
    color: var(--color-white);
    background: var(--gradient-main);

    box-shadow:
        0 12px 28px rgba(115, 87, 255, 0.25);
}


.btn-primary:hover,
.btn-primary:focus-visible {
    transform: translateY(-3px);

    box-shadow:
        0 18px 35px rgba(115, 87, 255, 0.32);
}


.btn-primary span {
    font-size: 1.1rem;
}


.hero-image-wrapper {
    position: relative;

    width: min(100%, 380px);

    margin-inline: auto;
}


.image-glow {
    position: absolute;

    width: 80%;
    height: 80%;

    left: 10%;
    top: 10%;

    border-radius: 50%;

    background: var(--gradient-main);

    filter: blur(45px);
    opacity: 0.23;
}


.hero-image-card {
    position: relative;
    z-index: 1;

    padding: 8px;

    border-radius: 34px;

    background:
        linear-gradient(
            145deg,
            rgba(255, 255, 255, 0.9),
            rgba(255, 255, 255, 0.35)
        );

    border: 1px solid rgba(255, 255, 255, 0.85);

    box-shadow: var(--shadow-card);

    transform: rotate(2deg);
}


.profile-image {
    width: 100%;
    aspect-ratio: 4 / 5;

    object-fit: cover;
    object-position: center top;

    border-radius: 28px;
}


.floating-badge {
    position: absolute;
    z-index: 3;

    display: flex;
    align-items: center;
    gap: 7px;

    padding: 9px 12px;

    border-radius: 12px;

    background: rgba(255, 255, 255, 0.86);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);

    border: 1px solid rgba(255, 255, 255, 0.8);

    color: var(--color-primary);

    font-size: 0.69rem;
    font-weight: 750;

    box-shadow: var(--shadow-soft);
}


.badge-top {
    top: 10%;
    left: -5%;
}


.badge-bottom {
    right: -4%;
    bottom: 10%;
}


.badge-top span {
    color: var(--color-accent);
}


.badge-bottom span {
    padding: 3px 6px;

    border-radius: 6px;

    background: var(--color-primary);
    color: var(--color-white);

    font-size: 0.62rem;
}


/* Decorative hero shapes */

.hero-shape {
    position: absolute;

    border-radius: 50%;

    pointer-events: none;
}


.hero-shape-one {
    width: 100px;
    height: 100px;

    top: 13%;
    right: -50px;

    border: 1px solid rgba(115, 87, 255, 0.2);
}


.hero-shape-two {
    width: 70px;
    height: 70px;

    bottom: 8%;
    left: -35px;

    background: rgba(24, 200, 181, 0.08);
}


/* =========================================================
   4. SECTION HEADINGS
========================================================= */

.section-heading {
    margin-bottom: 34px;
}


.section-heading h2 {
    max-width: 720px;

    color: var(--color-primary);

    font-size: clamp(2rem, 8vw, 3.6rem);
    line-height: 1.08;

    letter-spacing: -0.045em;
}


.section-heading h2 span {
    color: var(--color-secondary);
}


.section-heading.centered {
    text-align: center;
}


.section-heading.centered .section-label {
    justify-content: center;
}


.section-heading > p:last-child {
    max-width: 560px;

    margin-top: 14px;

    color: var(--color-text-soft);
}


.centered > p:last-child {
    margin-inline: auto;
}


/* =========================================================
   5. GLASS CARDS
========================================================= */

.glass-card {
    background:
        linear-gradient(
            135deg,
            rgba(255, 255, 255, 0.84),
            rgba(255, 255, 255, 0.57)
        );

    border: 1px solid rgba(255, 255, 255, 0.9);

    backdrop-filter: blur(18px);
    -webkit-backdrop-filter: blur(18px);

    box-shadow: var(--shadow-soft);
}


/* =========================================================
   6. ABOUT
========================================================= */

.about {
    background: var(--color-white);
}


.about-card {
    position: relative;

    display: grid;
    gap: 20px;

    padding: 28px;

    border-radius: var(--radius-lg);

    overflow: hidden;
}


.about-number {
    color: rgba(115, 87, 255, 0.14);

    font-size: 3.4rem;
    line-height: 1;

    font-weight: 900;
    letter-spacing: -0.06em;
}


.about-content {
    position: relative;
    z-index: 1;
}


.about-content p {
    color: var(--color-text-soft);

    font-size: 0.98rem;
}


.about-content p + p {
    margin-top: 17px;
}


/* =========================================================
   7. EDUCATION
========================================================= */

.education {
    background:
        linear-gradient(
            180deg,
            var(--color-white),
            var(--color-light)
        );
}


.education-card {
    padding: 26px;

    border-radius: var(--radius-lg);

    display: grid;
    gap: 25px;
}


.education-icon {
    width: 56px;
    height: 56px;

    display: grid;
    place-items: center;

    border-radius: 17px;

    background: var(--gradient-soft);

    font-size: 1.5rem;
}


.education-label {
    margin-bottom: 5px;

    color: var(--color-secondary);

    font-size: 0.75rem;
    font-weight: 800;

    letter-spacing: 0.1em;
    text-transform: uppercase;
}


.education-main h3 {
    color: var(--color-primary);

    font-size: clamp(1.7rem, 7vw, 2.4rem);
    line-height: 1.15;

    letter-spacing: -0.04em;
}


.education-description {
    max-width: 650px;

    margin-top: 10px;

    color: var(--color-text-soft);

    font-size: 0.94rem;
}


.education-details {
    display: grid;
    grid-template-columns: 1fr 1fr;

    border-top: 1px solid var(--color-border);
}


.detail-item {
    padding: 17px 8px;

    border-bottom: 1px solid var(--color-border);
}


.detail-item:nth-child(odd) {
    border-right: 1px solid var(--color-border);
}


.detail-item span {
    display: block;

    margin-bottom: 3px;

    color: var(--color-text-soft);

    font-size: 0.7rem;
    font-weight: 600;
}


.detail-item strong {
    display: block;

    color: var(--color-primary);

    font-size: 0.82rem;
}


/* =========================================================
   8. INTERESTS
========================================================= */

.interests {
    background: var(--color-off-white);
}


.interest-grid {
    display: grid;
    gap: 17px;
}


.interest-card {
    position: relative;

    min-height: 265px;

    padding: 26px;

    border-radius: var(--radius-lg);

    overflow: hidden;

    transition:
        transform var(--transition),
        box-shadow var(--transition);
}


.interest-card:hover {
    transform: translateY(-6px);
    box-shadow: var(--shadow-hover);
}


.interest-card-one {
    background:
        linear-gradient(
            145deg,
            #fff0f4,
            #ffffff
        );
}


.interest-card-two {
    background:
        linear-gradient(
            145deg,
            #f0f0ff,
            #ffffff
        );
}


.interest-card-three {
    background:
        linear-gradient(
            145deg,
            #eafbf8,
            #ffffff
        );
}


.interest-icon {
    width: 58px;
    height: 58px;

    display: grid;
    place-items: center;

    margin-bottom: 35px;

    border-radius: 17px;

    background: rgba(255, 255, 255, 0.75);

    box-shadow: 0 8px 25px rgba(16, 21, 47, 0.07);

    font-size: 1.55rem;
}


.interest-number {
    position: absolute;

    top: 22px;
    right: 24px;

    color: rgba(16, 21, 47, 0.16);

    font-size: 0.72rem;
    font-weight: 900;
}


.interest-card h3 {
    margin-bottom: 8px;

    color: var(--color-primary);

    font-size: 1.35rem;
    letter-spacing: -0.025em;
}


.interest-card p {
    color: var(--color-text-soft);

    font-size: 0.88rem;
}


/* =========================================================
   9. STRENGTHS
========================================================= */

.strengths {
    background: var(--color-white);
}


.strengths-grid {
    display: grid;
    gap: 12px;
}


.strength-card {
    display: flex;
    align-items: flex-start;
    gap: 16px;

    padding: 20px;

    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);

    background: var(--color-white);

    box-shadow: 0 8px 25px rgba(16, 21, 47, 0.04);

    transition:
        transform var(--transition),
        border-color var(--transition),
        box-shadow var(--transition);
}


.strength-card:hover {
    transform: translateX(4px);

    border-color: rgba(115, 87, 255, 0.25);

    box-shadow: var(--shadow-soft);
}


.strength-number {
    flex: 0 0 auto;

    width: 38px;
    height: 38px;

    display: grid;
    place-items: center;

    border-radius: 11px;

    background: var(--gradient-soft);

    color: var(--color-secondary);

    font-size: 0.7rem;
    font-weight: 850;
}


.strength-card h3 {
    margin-bottom: 4px;

    color: var(--color-primary);

    font-size: 1rem;
}


.strength-card p {
    color: var(--color-text-soft);

    font-size: 0.84rem;
    line-height: 1.55;
}


/* =========================================================
   10. CAREER GOALS
========================================================= */

.goals {
    padding-top: 60px;
    padding-bottom: 60px;

    background: var(--color-white);
}


.goal-card {
    position: relative;

    min-height: 390px;

    display: flex;
    align-items: center;

    padding: 35px 25px;

    border-radius: var(--radius-xl);

    overflow: hidden;

    background: var(--gradient-dark);

    box-shadow:
        0 25px 70px rgba(16, 21, 47, 0.22);
}


.goal-content {
    position: relative;
    z-index: 3;

    max-width: 720px;
}


.light-label {
    color: #b9b7ff;
}


.goal-card h2 {
    margin-bottom: 18px;

    color: var(--color-white);

    font-size: clamp(2rem, 8vw, 3.8rem);
    line-height: 1.05;

    letter-spacing: -0.045em;
}


.goal-card h2 span {
    display: block;

    background: var(--gradient-main);
    background-clip: text;
    -webkit-background-clip: text;
    color: transparent;
}


.goal-card p {
    max-width: 650px;

    color: rgba(255, 255, 255, 0.72);

    font-size: 0.94rem;
}


.goal-symbol {
    position: absolute;
    z-index: 1;

    right: -55px;
    bottom: -70px;

    width: 220px;
    height: 220px;

    display: grid;
    place-items: center;

    border-radius: 50%;

    background:
        linear-gradient(
            135deg,
            rgba(115, 87, 255, 0.38),
            rgba(255, 92, 138, 0.25)
        );

    border: 1px solid rgba(255, 255, 255, 0.08);
}


.goal-symbol span {
    color: rgba(255, 255, 255, 0.12);

    font-size: 3.7rem;
    font-weight: 900;
}


.goal-decoration {
    position: absolute;

    border-radius: 50%;

    filter: blur(5px);

    pointer-events: none;
}


.goal-decoration-one {
    width: 180px;
    height: 180px;

    top: -90px;
    right: 20%;

    background: rgba(115, 87, 255, 0.22);
}


.goal-decoration-two {
    width: 130px;
    height: 130px;

    bottom: -70px;
    left: 20%;

    background: rgba(255, 92, 138, 0.17);
}


/* =========================================================
   11. CONTACT
========================================================= */

.contact {
    background:
        radial-gradient(
            circle at 0% 100%,
            rgba(115, 87, 255, 0.10),
            transparent 30%
        ),
        var(--color-off-white);
}


.contact-card {
    display: grid;
    gap: 30px;

    padding: 25px;

    border-radius: var(--radius-lg);
}


.contact-icon {
    width: 52px;
    height: 52px;

    display: grid;
    place-items: center;

    margin-bottom: 16px;

    border-radius: 15px;

    background: var(--gradient-main);

    color: var(--color-white);

    font-size: 1.2rem;

    box-shadow: 0 10px 25px rgba(115, 87, 255, 0.2);
}


.contact-intro h3 {
    margin-bottom: 6px;

    color: var(--color-primary);

    font-size: 1.45rem;
}


.contact-intro p {
    max-width: 450px;

    color: var(--color-text-soft);

    font-size: 0.9rem;
}


.contact-links {
    display: grid;
    gap: 10px;
}


.contact-item {
    min-width: 0;

    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 12px;

    padding: 13px;

    border: 1px solid var(--color-border);
    border-radius: 15px;

    background: rgba(255, 255, 255, 0.65);

    transition:
        transform var(--transition),
        background var(--transition),
        box-shadow var(--transition);
}


.contact-item:hover {
    transform: translateY(-2px);

    background: var(--color-white);

    box-shadow: var(--shadow-soft);
}


.contact-item-icon {
    width: 38px;
    height: 38px;

    display: grid;
    place-items: center;

    border-radius: 11px;

    background: var(--gradient-soft);

    color: var(--color-secondary);

    font-weight: 800;
}


.contact-item small {
    display: block;

    color: var(--color-text-soft);

    font-size: 0.68rem;
    font-weight: 600;
}


.contact-item strong {
    display: block;

    min-width: 0;

    color: var(--color-primary);

    font-size: 0.78rem;

    overflow-wrap: anywhere;
    word-break: break-word;
}


.contact-arrow {
    color: var(--color-secondary);

    font-size: 1rem;
}


/* =========================================================
   12. FOOTER
========================================================= */

.site-footer {
    padding: 28px 0;

    background: var(--color-primary);

    color: rgba(255, 255, 255, 0.7);
}


.footer-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;

    text-align: center;
}


.footer-brand {
    display: inline-flex;
    align-items: center;
    gap: 9px;

    color: var(--color-white);

    font-size: 0.95rem;
    font-weight: 750;
}


.footer-brand .logo-mark {
    width: 32px;
    height: 32px;

    border-radius: 9px;

    font-size: 0.85rem;
}


.footer-content p {
    font-size: 0.76rem;
}


.footer-year {
    color: rgba(255, 255, 255, 0.42);
}


/* =========================================================
   13. FOCUS STATES
========================================================= */

a:focus-visible,
button:focus-visible {
    outline: 3px solid rgba(115, 87, 255, 0.35);
    outline-offset: 3px;
}


/* =========================================================
   14. TABLET — 600px+
========================================================= */

@media (min-width: 600px) {

    .container {
        width: min(
            calc(100% - 48px),
            var(--container-width)
        );
    }


    .section {
        padding: 95px 0;
    }


    .hero {
        padding-top: 135px;
    }


    .hero-content {
        gap: 65px;
    }


    .hero-image-wrapper {
        width: min(100%, 430px);
    }


    .about-card {
        padding: 36px;
    }


    .education-card {
        padding: 34px;
    }


    .interest-grid {
        grid-template-columns: repeat(2, 1fr);
    }


    .interest-card-three {
        grid-column: 1 / -1;
    }


    .strengths-grid {
        grid-template-columns: repeat(2, 1fr);
    }


    .strength-card:last-child {
        grid-column: 1 / -1;
    }


    .goal-card {
        padding: 50px;
    }


    .contact-card {
        padding: 34px;
    }
}


/* =========================================================
   15. DESKTOP — 850px+
========================================================= */

@media (min-width: 850px) {

    .section {
        padding: 115px 0;
    }


    .navbar {
        min-height: 82px;
    }


    .menu-toggle {
        display: none;
    }


    .nav-links {
        position: static;

        flex-direction: row;
        align-items: center;

        padding: 0;

        background: transparent;

        border: 0;
        box-shadow: none;

        opacity: 1;
        visibility: visible;
        transform: none;
    }


    .nav-links a {
        padding: 8px 11px;

        font-size: 0.82rem;
    }


    .hero {
        min-height: 100svh;

        padding-top: 120px;
    }


    .hero-content {
        grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.75fr);
        align-items: center;

        gap: 70px;
    }


    .hero-image-wrapper {
        width: min(100%, 450px);
        justify-self: end;
    }


    .hero h1 {
        font-size: clamp(4rem, 7vw, 6.5rem);
    }


    .about-card {
        grid-template-columns: 100px minmax(0, 1fr);
        gap: 30px;

        padding: 45px;
    }


    .about-number {
        font-size: 4rem;
    }


    .education-card {
        grid-template-columns: auto minmax(0, 1fr);
        column-gap: 28px;
        align-items: start;
    }


    .education-icon {
        grid-row: span 2;
    }


    .education-details {
        grid-column: 1 / -1;

        grid-template-columns: repeat(4, 1fr);

        margin-top: 12px;
    }


    .detail-item {
        border-bottom: 0;
        border-right: 1px solid var(--color-border);
    }


    .detail-item:nth-child(odd) {
        border-right: 1px solid var(--color-border);
    }


    .detail-item:last-child {
        border-right: 0;
    }


    .interest-grid {
        grid-template-columns: repeat(3, 1fr);
    }


    .interest-card-three {
        grid-column: auto;
    }


    .strengths-grid {
        grid-template-columns: repeat(3, 1fr);
    }


    .strength-card:last-child {
        grid-column: 2 / 4;
    }


    .goal-card {
        min-height: 450px;

        padding: 65px;
    }


    .contact-card {
        grid-template-columns: 0.8fr 1.2fr;
        align-items: center;

        padding: 45px;
    }


    .footer-content {
        flex-direction: row;
        justify-content: space-between;

        text-align: left;
    }
}


/* =========================================================
   16. LARGE DESKTOP — 1100px+
========================================================= */

@media (min-width: 1100px) {

    .hero-content {
        gap: 110px;
    }


    .hero-image-wrapper {
        width: 470px;
    }


    .about-card {
        padding: 55px;
    }


    .interest-card {
        min-height: 300px;
        padding: 30px;
    }


    .strengths-grid {
        gap: 16px;
    }


    .strength-card {
        padding: 24px;
    }


    .goal-card {
        padding: 75px;
    }
}


/* =========================================================
   17. REDUCED MOTION
========================================================= */

@media (prefers-reduced-motion: reduce) {

    html {
        scroll-behavior: auto;
    }


    *,
    *::before,
    *::after {
        transition-duration: 0.01ms !important;
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
    }
}


/* =========================================================
   18. VERY SMALL PHONES
========================================================= */

@media (max-width: 360px) {

    .container {
        width: min(
            calc(100% - 24px),
            var(--container-width)
        );
    }


    .section {
        padding: 65px 0;
    }


    .hero {
        padding-top: 110px;
    }


    .hero h1 {
        font-size: 2.7rem;
    }


    .hero-tagline {
        font-size: 1.05rem;
    }


    .hero-subtitle {
        font-size: 0.9rem;
    }


    .btn {
        width: 100%;
    }


    .hero-image-wrapper {
        width: min(100%, 320px);
    }


    .floating-badge {
        font-size: 0.62rem;
    }


    .badge-top {
        left: -2%;
    }


    .badge-bottom {
        right: -2%;
    }


    .education-details {
        grid-template-columns: 1fr;
    }


    .detail-item,
    .detail-item:nth-child(odd) {
        border-right: 0;
    }


    .detail-item:last-child {
        border-bottom: 0;
    }


    .contact-card {
        padding: 20px;
    }


    .contact-item strong {
        font-size: 0.7rem;
    }
}
